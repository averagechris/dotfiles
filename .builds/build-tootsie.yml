# Build tootsie NixOS configuration (inactive - Linode VM)
image: nixos/unstable
packages:
  - nixos.cachix
sources:
  - https://git.sr.ht/~averagechris/dotfiles
secrets:
  - 731f7e55-4497-4228-8fa4-1657da7d3625
tasks:
  - setup: |
      mkdir -p ~/.config/nix/
      echo "experimental-features = nix-command flakes" >> ~/.config/nix/nix.conf
      echo "max-jobs = auto" >> ~/.config/nix/nix.conf
      cachix authtoken --stdin < ~/.ci_secrets/cachix_token
      cachix use averagechris-dotfiles

  - build: |
      cd ~/dotfiles
      echo "Building tootsie..."
      cachix watch-exec averagechris-dotfiles -- \
          nix build --accept-flake-config \
          .#nixosConfigurations.tootsie.config.system.build.toplevel
