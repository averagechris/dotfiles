# Build tom NixOS configuration (home-assistant host)
image: nixos/unstable
packages:
  - nixos.cachix
sources:
  - https://git.sr.ht/~averagechris/dotfiles
secrets:
  - 731f7e55-4497-4228-8fa4-1657da7d3625
tasks:
  - setup: |
      source ~/dotfiles/scripts/ci-setup.sh

  - build: |
      cd ~/dotfiles
      echo "Building tom..."
      cachix watch-exec averagechris-dotfiles -- \
          nix build --accept-flake-config \
          .#nixosConfigurations.tom.config.system.build.toplevel
