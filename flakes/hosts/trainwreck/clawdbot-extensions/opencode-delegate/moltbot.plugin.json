{
  "id": "opencode-delegate",
  "name": "OpenCode Delegate",
  "version": "1.0.0",
  "description": "Delegate complex coding tasks to opencode AI assistant for multi-file edits, refactoring, debugging, and more",
  "extensions": ["./index.ts"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "defaultWorkdir": {
        "type": "string",
        "description": "Default working directory for coding tasks (defaults to ~/dotfiles)"
      },
      "defaultModel": {
        "type": "string",
        "description": "Default model to use (e.g., 'anthropic/claude-sonnet-4-20250514')"
      },
      "defaultTimeout": {
        "type": "number",
        "description": "Default timeout in seconds (default: 300, max: 600)"
      }
    }
  }
}
