-- prototyping.lua - WezTerm ad-hoc configuration experiments
-- Copy this file to ~/.config/wezterm/prototyping.lua and modify for testing
-- Changes here are loaded LAST and override all other settings

-- Get access to the wezterm API
local wezterm = require 'wezterm'

-- Create a module with just the apply function
-- @type {apply: function}
local module = {
  -- This apply function is how you should make changes
  -- All prototype settings should go inside this function
  -- @param config table The WezTerm configuration table to modify
  apply = function(config)
    -- UNCOMMENT SETTINGS BELOW TO TRY THEM OUT
    
    -- Change colors
    -- config.color_scheme = "Dracula"
    
    -- Change font sizes
    -- config.font_size = 14.0
    
    -- Tab bar settings
    -- config.hide_tab_bar_if_only_one_tab = false
    
    -- Window appearance
    -- config.window_background_opacity = 0.9
    
    -- Add keybindings
    -- table.insert(config.keys, { 
    --   key = "E", mods = "CTRL|SHIFT", 
    --   action = wezterm.action.EmitEvent("prototype-event") 
    -- })
    
    -- Test ligatures or other font features
    -- config.harfbuzz_features = {"calt=1", "clig=1", "liga=1"}
    
    -- ADVANCED: Add custom callback to run code when WezTerm starts
    -- wezterm.on("gui-startup", function()
    --   wezterm.log_info("WezTerm started with prototype config!")
    -- end)
    
    wezterm.log_info("✨ Prototype config loaded and applied!")
  end,
}

-- Return the module
return module