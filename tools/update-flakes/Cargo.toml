[package]
name = "dotfiles-update-flakes"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "update-flakes"
path = "src/main.rs"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
