# use sort from coreutils if installed else use a less robust flag combination of field
# flags for bsd sort
which gsort &> /dev/null \
    && gsort -V $@ \
    || sort -t. -k 1,1n -k 2,2n -k 3,3n -k 4,4n $@
